extends layouts/main-layout.pug

block content
    .content
        h1 List of Users
        if users
        ul
            each user in users
                li #{user}
        a(href="/") <- Add new User
